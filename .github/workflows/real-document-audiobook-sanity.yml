name: Real Document Audiobook Sanity

on:
  workflow_dispatch:

jobs:
  manual-ai-audiobook-sanity:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    env:
      DOCXAI_RUN_REAL_DOCUMENT_AUDIOBOOK_SANITY: "1"
      OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Require OpenAI secret
        run: |
          if [ -z "${OPENAI_API_KEY:-}" ]; then
            echo "OPENAI_API_KEY secret is required for Real Document Audiobook Sanity"
            exit 1
          fi

      - name: Install system dependencies
        run: |
          sudo apt-get update
          grep -vE '^\s*(#|$)' system-requirements.apt | xargs -r sudo apt-get install -y

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          python -m venv .venv
          . .venv/bin/activate
          pip install -r requirements.txt

      - name: Run translate plus audiobook postprocess sanity
        run: |
          bash scripts/test.sh tests/test_real_document_audiobook_spec.py -vv

      - name: Upload audiobook sanity artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: real-document-audiobook-sanity-artifacts
          if-no-files-found: ignore
          path: |
            .run/**
            tests/artifacts/real_document_pipeline/**
