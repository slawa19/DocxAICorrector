# Handoff — 2026-07-11 (pre-UI complete, next: UI slice)

> **HISTORICAL SNAPSHOT (as of 2026-07-11).** Its status lines are frozen in time and now stale — e.g. it lists
> specs 001–012 and a "1966 passed" count, whereas specs now run to 021 (spec 012 still `ACTIVE`) and the suite has
> grown well past that number. Do not treat the spec/test figures here as current. For live status see the RESUME
> POINT in `docs/specs/GLOBAL_PLAN_2026-06-16.md`.

Purpose: resume this project in a fresh session. The authoritative live status is the **RESUME POINT block at
the top of `docs/specs/GLOBAL_PLAN_2026-06-16.md`**; this file is the session handoff + paste-ready prompt.

## Paste-ready prompt for a new session

```
Продолжаем DocxAICorrector. Вся до-UI работа завершена, влита в main и
провалидирована на 4 реальных книгах (см. память + docs/specs/GLOBAL_PLAN_2026-06-16.md,
блок RESUME POINT наверху — он authoritative). Следующая задача — реализовать
первый UI-слайс строго по переписанной спеке
docs/specs/UI/FORMATTING_DISCREPANCY_REPORTING_SPEC_2026-06-15.md.

Это presentation-only слой поверх УЖЕ существующего контракта (данные и review-файл
готовы и покрыты тестами — не строить их заново, не трогать пайплайн). Прочитай
UI-спеку, затем предложи план первого инкремента и веди через агентов проверки/
реализации, сам оставайся оркестратором. Придерживайся конституции
.specify/memory/constitution.md (особенно VII универсальность и VIII свежесть
доказательств), пиши спеку под specs/013-*, проверяй через WSL bash scripts/test.sh,
держи pyright-храповик ≤244, вливай в main после каждого завершённого прохода.

Начни с чтения UI-спеки и памяти, потом дай план — не начинай код без согласования плана.
```

## Repository state

- Branch `main`, clean, all pushed. Last merge on 2026-07-11: `f3062ec`. Every pass this session was merged as a
  separate `--no-ff` merge.
- pyright ratchet baseline **244** (`tests/test_typecheck.py`, `_ERROR_BASELINE`) — do NOT edit it.
- Full suite: `wsl.exe -d Debian --cd "D:\www\projects\2025\DocxAICorrector" -- bash scripts/test.sh` (green: 1966 passed, 13 skipped).

## What is done (specs 001–012, all in main)

- 001 heading-role-preservation · 002 gate-report-honesty (3-state verdict) · 003 list-fragment-detector ·
  004 emphasis-coverage-advisory · 005 hygiene-pass-safety · 006 gate-on-delivered-markdown ·
  007 list-marker-from-role (**SHELVED** — unverifiable under non-deterministic translation) ·
  008 paragraph-break-detection (**IMPLEMENTED**, advisory) · 009 controlled-fallback-non-completed-response ·
  010 production-acceptance-semantics (**decision**: coverage = review-data, not a gate) ·
  011 unmapped-target-review-items · 012 remove-legacy-test-sources.
- **Real-book validation (2026-07-11):** Money / Mazzucato / Creating Wealth / Lietaer — all acceptance passed,
  gate honest, no text loss, chapters/parts as correct-level headings, specs 001–011 hold live. **Cleared to UI.**
  (Run ids `20260711T_{money,mazzucato,creatingwealth,lietaer}_real1` — artifacts under `.run`/runs, gitignored.)

## Next task — the UI slice

Implement the FIRST increment per the rewritten spec
`docs/specs/UI/FORMATTING_DISCREPANCY_REPORTING_SPEC_2026-06-15.md`. It is a **presentation-only** front-end over
an already-shipped contract — do NOT rebuild the data or the writer, do NOT touch the pipeline.

Minimal scope: a final notice keyed on MAX severity; counts `[КРИТ]/[ПРАВКА]/[ПРОВЕРКА]`; a download control for
`<stem>.result.formatting_review.txt`; copy that a successful (accepted) result can still carry review items;
`short_note_or_marker` softening; NO internal jargon (`role_aware`/`retained residue`/`gate_source`); DOCX always
downloadable regardless of severity. Non-goals: no inline navigator, no auto-fix of Word styles, no per-sample
expandable cards, no new analysis/writer.

### Contract the UI reads (verified 2026-07-11, already exists)

- Object: `quality_warning.formatting_review_items` inside the delivered `<stem>.result.meta.json`. It exists
  **only when `quality_status ∈ {warn, fail}`** — on a clean pass there is no `quality_warning` (show "clean").
  Built by `_build_result_quality_warning` (`late_phases.py:3687-3707`); persisted at `runtime/artifacts.py:199-200`.
- Item shape (`_build_formatting_review_item`, `late_phases.py:2301-2344`):
  `{reason, label(RU), count, severity ∈ {fix, review, defect}, aggregate_count?, action_style?, sample?}`.
- Severity → marker (`runtime/artifacts.py:63`): `fix→[ПРАВКА]`, `review→[ПРОВЕРКА]`, `defect→[КРИТ]`.
- Artifacts: `.run/ui_results/`, stem `<YYYYMMDD_HHMMSS>_<name>.result`, files
  `.docx / .md / .formatting_review.txt / .meta.json`. Writer + retention (≤80 groups / ≤7 days) + tests already
  exist: `runtime/artifacts.py:88-263`, `tests/test_runtime_artifacts.py`, `tests/test_runtime_artifact_retention.py`.
- Two axes (spec 010): `acceptance_passed` (pass/fail on structure/hygiene) is SEPARATE from the review-data;
  a run can be `acceptance_passed=true` AND `quality_status=warn` (has review items). Do not conflate.
- The ONE possible code touch: a copy tweak for `short_note_or_marker` in `_build_formatting_review_text`
  (`runtime/artifacts.py:88-169`) — data-preserving; everything else is pure front-end.

## How to run a real book (expensive; one at a time; only when necessary)

```
wsl.exe -d Debian -- bash -lc 'cd /mnt/d/www/Projects/2025/DocxAICorrector && . .venv/bin/activate && \
  export PYTHONPATH=src:. && \
  export DOCXAI_REAL_DOCUMENT_PROFILE=money-sustainability-pdf-full-heldout && \
  export DOCXAI_REAL_DOCUMENT_RUN_PROFILE=ui-parity-translate-benchmark-advisory-image-safe-no-cleanup && \
  export DOCXAI_REAL_DOCUMENT_FORCED_RUN_ID=<id> && \
  python -u tests/artifacts/real_document_pipeline/run_lietaer_validation.py'
```

- Canonical corpus = 4 profiles pointing at `tests/sources/book/`: `money-sustainability-pdf-full-heldout`,
  `lietaer-pdf-full-benchmark`, `mazzucato-pdf-full-benchmark`, `creatingwealth-pdf-full-benchmark`.
- Registry: `corpus_registry.toml` (repo root). Model = Gemini via OpenRouter (see `docs/RUNNING_THE_PIPELINE.md`;
  verify model+key with the cheap live check there BEFORE an expensive run). Legacy `tests/sources/archive/`
  sources were removed (spec 012) — do not resurrect them.

## Guardrails (constitution `.specify/memory/constitution.md`)

- WSL Debian is the runtime source of truth; run tests via `bash scripts/test.sh`.
- VII — universal rules only, no per-book literals/heuristics; a defect with no general rule is ACCEPTED, not
  patched. TOC and footnotes are out of scope.
- VIII — evidence fresher than the fix; verify against the delivered artifact (open the DOCX), not the report.
- Minimal, bounded changes; spec before non-trivial work (`specs/<NNN>-<slug>/`, with Non-goals + Anti-regression).
- Accepted tails (do NOT chase): a few stray-char front-matter/OCR headings (`%`, `О`, page digits),
  untranslated proper names (currency names, publishers), untranslated author bios, one English chapter marker.
  All pass-through, honestly reported, do not block.

## Memory pointers (auto-loaded)

`MEMORY.md` index → `gate-quality-workstream.md` (full workstream log), `quality-philosophy.md`,
`gate-checks-a-different-document.md`, `ci-and-typecheck-policy.md`.
