# Handoff — 2026-07-11 (expired)

> **This handoff is out of date. Do not start a session from it.** For where the project actually stands,
> read `docs/WHERE_WE_ARE.md`; the source of truth is the code and the git history.
>
> It used to carry a ready-to-paste prompt telling the next session to build the UI. That prompt has been
> removed: the UI was built by `specs/013-ui-minimal-screen-and-result/spec.md` and merged into `main` on
> 2026-07-13 (commit 58aaf2a). Anyone who pasted it would have redone finished work.

---

## Historical snapshot — state on 2026-07-11

Kept as a record of that day only. Every number below is frozen in time and has since moved on.

- Branch `main`, clean, all pushed; last merge that day `f3062ec`.
- Specs 001–012 were in `main`: 001 heading-role-preservation · 002 gate-report-honesty (3-state verdict) ·
  003 list-fragment-detector · 004 emphasis-coverage-advisory · 005 hygiene-pass-safety ·
  006 gate-on-delivered-markdown · 007 list-marker-from-role (shelved) · 008 paragraph-break-detection
  (advisory) · 009 controlled-fallback-non-completed-response · 010 production-acceptance-semantics
  (decision: coverage is review data, not a gate) · 011 unmapped-target-review-items ·
  012 remove-legacy-test-sources.
- Real-book validation that day: Money / Mazzucato / Creating Wealth / Lietaer — all passed acceptance, the
  gate reported honestly, no text loss, chapters and parts landed as headings of the right level.
  (Run ids `20260711T_{money,mazzucato,creatingwealth,lietaer}_real1`; artifacts were gitignored and are gone.)
- Full suite at that point: 1966 passed, 13 skipped. The pyright ratchet baseline was 244.
- The decision recorded that day was "cleared to start the UI slice" — which is exactly what happened next,
  in spec 013.

For anything operational, use the current docs instead of this file: `docs/RUNNING_THE_PIPELINE.md` for
running the pipeline, `.specify/memory/constitution.md` for the binding rules, and `AGENTS.md` for the
agent-facing contracts.
