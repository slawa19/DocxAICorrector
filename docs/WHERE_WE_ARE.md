# Где мы сейчас

**Когда этот файл обновляли в последний раз, скажет git, а не строка в шапке:**

```bash
git log -1 --format=%ad --date=short -- docs/WHERE_WE_ARE.md
```

Раньше здесь стояла приколоченная дата, и она устарела ровно тем способом, от которого этот файл
должен защищать: восемь правок 2026-08-07 не тронули её, и читатель видел «обновлено 2026-08-03» над
разделами, датированными четырьмя днями позже. Дата обновления — значение, меняющееся при каждом
слиянии, а такие значения здесь не хранятся (см. следующий раздел).

## Как читать этот файл

Это **навигация, а не источник правды**. Источник правды — код и `git`. Документы в этом
репозитории уже один раз соврали дорого: сессия 2026-07-31 начала планирование с
`docs/specs/GLOBAL_PLAN_2026-06-16.md`, прочитала там «дальше — UI-слайс» и предложила владельцу
делать UI, который на самом деле был сделан и влит месяцем раньше (спека 013). Поэтому здесь нет
пересказа состояния — здесь команды, которыми состояние проверяется за десять секунд, и список
мест, где документация уже расходилась с реальностью.

Правило: **если этот файл противоречит `git log`, прав `git log`.** Если заметили расхождение —
поправьте этот файл тем же PR, в котором заметили.

### Почему здесь нет ни хеша текущего `main`, ни чисел тестов

Первая редакция этого файла (2026-07-31) начиналась строкой «`main` = такой-то хеш» и приводила
здоровье проекта числами вида «столько-то тестов прошло». Через сутки оба утверждения были ложными:
их опроверг первый же влитый PR. Документ, написанный против устаревания документации, устарел за
день — и ровно тем способом, от которого он должен был защищать.

Вывод, который здесь и зафиксирован: **значения, меняющиеся при каждом слиянии, в этом файле не
хранятся вообще.** Вместо значения хранится команда, которая его выдаёт (следующий раздел). Если у
числа есть законный владелец в репозитории — число живёт у владельца, а сюда попадает только ссылка
на него: эталон pyright, например, это константа `_ERROR_BASELINE` в `tests/test_typecheck.py`, и
тест падает и при её превышении, и при снижении, так что разойтись с реальностью она не может.

Исключение — хеши, названные как исторический факт: «UI влит коммитом `58aaf2a` 2026-07-13».
Прошлое не протухает, и такие ссылки полезны.

Утверждения о состоянии, которые нельзя свести к команде («владелец принял такое-то решение»),
всегда идут с датой. Дата — сигнал читателю: чем она старше, тем внимательнее перепроверяйте.

Гард `tests/test_where_we_are_freshness.py` не даёт вернуть сюда счётчики тестов и приколоченный
хеш `main`. Он ловит именно ту форму дрейфа, что случилась, и ничего больше — оценивать
осмысленность прозы автоматика не умеет, это остаётся на ревью.

## Проверить состояние: команды вместо чисел

Всё запускается **только в WSL**. `scripts/test.sh` из Git Bash под Windows подхватывает системный
Python и даёт ложные падения.

```bash
git log --oneline -5 main                        # что влито последним
bash scripts/test.sh tests/ -q                   # полный набор тестов
bash scripts/test.sh tests/test_typecheck.py -q  # храповик pyright: падает и при росте, и при снижении числа ошибок
bash scripts/run-real-document-quality-gate.sh   # канонический гейт реальных документов, четыре книги корпуса (~15 минут)
```

## Проверить статус любой спеки

Не читайте поле `Status:` в `specs/NNN-*/spec.md` как истину — оно исторически отставало на месяцы.
Проверяйте так:

```bash
git log --oneline main --grep="spec 043"       # есть ли работа по спеке в main
git merge-base --is-ancestor <commit> main     # влит ли конкретный коммит
git log --oneline main..<branch>               # что есть в ветке и ещё не в main
```

Статусы спек 012–053 были выверены и приведены в соответствие 2026-07-31…08-01. Если вы читаете это
сильно позже — перепроверьте, дрейф возобновляется.

Гард `tests/test_spec_status_consistency.py` требует, чтобы спека, чей статус означает «работа
открыта», была упомянута в разделе «Что открыто» ниже. Он не проверяет обратное: закрытая спека
может упоминаться здесь как контекст.

**Тот же гард с 2026-08-07 ходит в `git`, и это стоит знать до того, как вы что-то напишете или
закоммитите.** Раньше он сверял документы только друг с другом, поэтому спеку 056 поймать не мог: оба
документа врали согласованно. Второе правило: ни один живой документ в корне и в `docs/` не вправе
утверждать «не начато» о спеке, по которой в достижимой истории уже есть коммит, трогающий `src/`,
`tests/`, `scripts/` или `resources/` (`docs/archive/` исключён — там это заморожено в 2026-06). Фраза
в кавычках читается как цитата прошлой формулировки, а не как утверждение, — иначе исправленный
документ не мог бы рассказать, что именно он исправил. Три уточнения, за каждое из которых заплачено
красным CI:

- **Работа относится к спеке по ТЕМЕ коммита, а не по телу** (`2519777`, 2026-08-08). Тело — проза,
  оно называет чужие номера для контекста и даже чтобы связь отрицать, а отрицания матчер прочитать не
  может: CI упал дважды подряд на коммите, чьё тело говорило «к этой спеке работа не относится».
  Замерено по всей истории — у спек 054–058 сорок семь коммитов называют их в теме и ни один только в
  теле, так что сужение не потеряло ни одного истинного отнесения. Практический вывод для пишущего
  коммит: **не ставьте номер спеки в тему, если коммит не является работой по ней** — уроните CI.
- **«Историю не достать» и «история коротка» — разные отказы** (`b9b7f80`, 2026-08-08). Мелкий клон —
  это РАБОТАЮЩИЙ репозиторий с пустым ответом, и пройти на нём значит не проверить ничего: падение.
  Checkout, который git вообще не может открыть, — дефект окружения: пропуск с объяснением. Ровно
  один такой случай здесь встречался — боковой worktree, созданный windows-git, оставляет `.git`
  файлом с записью `gitdir:` по windows-пути, и WSL-git его не резолвит. Различение узкое, по тексту
  ошибки git; пустая и короткая история падают как раньше.
- Поэтому job `tests` в `.github/workflows/ci.yml` берёт репозиторий с `fetch-depth: 0`, а
  `scripts/docker-ci-parity.sh` перестал исключать `.git`. Job `editable-install` оставлен мелким
  осознанно.

## Что закрыто

Спеки 001–053, кроме перечисленного ниже (спека 017 закрыта 2026-08-02). Крупные вехи: пайплайн перевода книг работает на четырёх
реальных полных книгах; канонический гейт реальных документов честно зелёный; UI-слайс с отчётом о
расхождениях форматирования сделан (спека 013); волна hardening под SaaS (022–028) и пять
декомпозиций крупных модулей (031–035) закрыты; ревью-раунды 3–12 закрыты (036–043, 051).

**Спека 057 закрыта 2026-08-07 прогоном «до/после» на одной книге.** Восстановление границ больше не
разрушает границу, которую сам исходник помечает завершающим знаком: отказ опирается на источник,
считается отдельным счётчиком `source_terminal_denials` и не трогает озвучку — та исполняет другую
функцию (`_join_narration_sentence_continuations`). Живьём: потерянных границ стало 53 вместо 80,
доставленных абзацев на 27 больше, текст не потерян, acceptance passed. Две вещи оттуда стоит помнить
за пределами спеки: импортёр запекает markdown-выделение внутрь текста абзаца
(`document/extraction.py:1383-1387`), поэтому любое правило, читающее последний символ ИСХОДНОГО
абзаца, обязано сперва развернуть хвост — иначе оно не срабатывает ни разу; и пятиабзацный блок
подписей остался слитым, потому что в тех строках знаков препинания нет вовсе (конституция VII).

**Спека 058 закрыта 2026-08-07 прогоном «до/после» на двух книгах, где правило применимо.**
Продолжение 057 с другим сигналом: границы, помеченные не пунктуацией, а РОЛЬЮ источника. Из 391
поглощённого абзаца по четырём книгам 72 несли структурную роль. Построена половина A — подпись к
рисунку, вваренная в плейсхолдер картинки; отказ опирается на роли `image`/`caption` и стоит в той же
точке принятия слияния, что и 057, счётчик `source_role_denials`. Живьём: **классы `caption` и
`image` исчезли из поглощённых полностью** — было 20 и 19, стало 0 и 0, ровно как предсказано;
доставленных абзацев на 21 и 19 больше, текст не потерян, acceptance не изменился, склейки озвучки не
упали. Три вещи оттуда стоит помнить за пределами спеки: `_entry_is_protected_boundary` обходится
безусловно через ветку `false_fragment_heading` (`pipeline/output_validation.py:1297`,
`_entries_match_allowed_protected_merge`; обход — на `:1305-1307`), а
`protected_boundary_denials` инкрементируется на `:1420`, то есть ДО предикатов формы (`:1426`), — поэтому новые отказы туда ставить
нельзя; и половина B не построена намеренно, потому что 28 из 32 поглощённых заголовков — ошибки роли
из импорта.

**Спека 055 — мост PDF→DOCX. ЗАКРЫТА со статусом `MEASURED` 2026-08-05/06, а не «не начата».**
Этот раздел до 2026-08-07 утверждал «не начато», хотя работа была сделана и влита (PR #43, #44:
`25eb424`, `3472958`, `6590f1c`, `24818b5`). Гард этого не поймал: он требует, чтобы ОТКРЫТАЯ спека
упоминалась здесь, но не требует убирать отсюда закрытую. Расхождение нашло независимое ревью, а
не тест. **С 2026-08-07 ловит другой гард** — в `tests/test_spec_status_consistency.py` добавлено
правило «ни один документ не вправе сказать «не начато» о спеке, по которой в истории уже есть
рабочие коммиты»; асимметрию выше закрывать не стали, она ловит только этот случай и не ловит 056.
Чем кончился эксперимент: из трёх сигналов два (кегль и отступ первой строки) проведены
через мост и оставлены; третий, выключка, проведён, измерен, **опровергнут корпусом и удалён** —
геометрический тест на центрирование дал 33 ложных повышения заголовка из 33. Роли `caption`
выросли 19 → 53. Топология абзацев не изменилась. Прецедент тот же, что у спеки 049: идею
реализовали, замерили и откатили — и это записано как решение, а не как случайность.

**Мёртвый UI-код удалён 2026-08-07/08. Спеки у этой работы нет — она defect-driven.** Панель
compare-all (`ui/compare_panel.py`) возвращала `None` на любом входе, и три теста пиннили это ничто
(`e3e7d5d`); четыре символа в `_ui.py` не звал никто, а `render_result` импортировался и не
вызывался — завершённый вид идёт через `render_result_bundle` (`72d6ff7`); два помощника в
`generation/message_formatting.py` дублировали каталог сообщений байт-в-байт, а ключ `common.download`
не читал ни один `.py` (`2407697`). Отдельно ушла **лента активности целиком** (`3074553`, PR #65) —
331 ссылка в 38 файлах: кольцевой буфер на 20 записей, которого не читал ни один экран, и весь канал
за ним — `PushActivityEvent`, шов `emit_activity` на обоих рантаймах, протокол `ActivityEmitter`,
ключи `app.activity_*` и полсотни производителей, раздававших туда захардкоженные русские строки мимо
каталога сообщений. Видимый пользователю `run_log` не тронут. Заодно снят замок в
`tests/test_ui_i18n.py`: тест на fallback держал `en.json` в заложниках одним ключом, поэтому дописать
английский перевод значило уронить сборку (`ca47dfb`).

**Оплаченный результат больше не уничтожается молча — двумя разными правками, потому что триггера
два.** Смена целевого языка, языка оригинала или режима обработки входит в маркер подготовки и
обнуляла выданный результат первой же строкой `start_background_preparation`; там теперь
подтверждение, потому что настройка на результат не направлена и единственный честный момент
заговорить — диалог (`29f207f`, PR #61). Клик по кнопке повторного старта направлен ровно сюда, и там
вместо подтверждения стоит предупреждение, висящее рядом с кнопками скачивания с момента появления
результата (`10f9858`, PR #66): человек уже попросил новый прогон, переспрашивать его не о чем.
Предикат один на оба входа — `start_discards_delivered_result`.

**Все отслеживаемые текстовые файлы перенормализованы в LF 2026-08-08** (`3c6f7f7`, PR #67): 65 файлов
лежали в индексе с CRLF вопреки `.gitattributes`, и любой инструмент, переписывающий такой файл
целиком, размазывал по диффу восемьдесят с лишним призрачных строк — на это натыкались дважды за день.
Содержимое не изменилось ни на байт. Момент выбран намеренно: все ветки дня были влиты, параллельных
деревьев не осталось, иначе диф в 3751 строку передрался бы с ними.

Здоровье проекта проверяется четырьмя командами из раздела выше, и числа отсюда намеренно убраны.
Что известно точно: 2026-08-02 на ветке спеки 017 прогонялись полный набор тестов, статический ярус,
храповик pyright и канонический гейт реальных документов — падений нет. После этого в `main` влиты
ещё правки; если ответ нужен сейчас, запустите команды сами, это быстрее, чем искать, кому верить.

## Что открыто

**Спека 059 — проза теряется в обоих путях, и экран об этом молчит. Написана 2026-08-07/08; ступени 2
и 3 лестницы влиты и подтверждены платным прогоном 2026-08-08, остальное открыто.** Требование
владельца: текст книги переведён
ПОЛНОСТЬЮ, сноски и оглавление не в счёт; ноль
недостижим для форматирования, но не для перевода. Сегодня теряют оба пути: в DOCX уезжает английский
исходник, а из озвучки текст **выбрасывается** — ветка `pipeline/block_execution.py:988-991` внутри
`continue_controlled_processed_block_rejection` не делает ни одного `narration_chunks.append`
(единственный в этой функции стоит в `else`, `:1005`), так что на прогоне
2026-08-06 из аудиокниги Money & Sustainability пропало 5 581 символов сверх 59 законно исключённых
блоков. **Ноль достижим:** `marker_validation_source_fallback` — отказ ПАКЕТИРОВАНИЯ, а не перевода;
`enable_paragraph_markers` по умолчанию False (`core/config_runtime_sections.py:541`), а при
`marker_mode=False` валидатор возвращает ответ нетронутым (`generation/_generation.py:1035-1040`), так
что при переспросе абзацев поодиночке класс ошибки недостижим по построению. Часть A — лестница из
четырёх ступеней (блок с маркерами → абзац без маркеров → предложения → **другая модель**), после
которой состояния «этот абзац нельзя перевести» не существует, плюс инвариант: в
`CONTROLLED_BLOCK_FAILURE_POLICY` (`pipeline/block_execution.py:20-38`) архитектура уже делит отказы на
`fallback_continue` и `fail`, и семь качественных отказов на абзаце ПРОЗЫ теряют право на первое —
доставка непереведённой прозы становится невыразимым исходом. Верхняя граница цены — **≈11% прогона,
≈0,06 $**. Честный остаток — недоступность провайдера, и это не «не смогли перевести абзац», а
«прогона не было». Часть B — сигнализация: `acceptance_verdict` считается и кончается в
`.run/quality_reports/*.json`, в `src/docxaicorrector/ui/` слово `acceptance` не встречается ни разу,
а весь гигиенический гейт живёт под `if processing_operation == "translate"`
(`pipeline/quality_gate.py:1351-1678`), поэтому в `edit` (дефолт) и `audiobook` экран показывает
только «Документ обработан.» Часть B проверяема без платного прогона; часть A требовала одного, и он
сделан — см. ниже.

Что из этого влито 2026-08-08. **Ступени 2 и 3** — `_attempt_degradation_ladder` в
`generation/_generation.py` (`054e405`, PR #68): отвергнутый блок сперва переспрашивается абзацами с
`marker_mode=False`, а при отказе по РАЗМЕРУ делится по предложениям со склейкой обратно в тот же
абзац, и только потом отдаётся четырём подстановкам. Глубина ровно одна, вложенные вызовы идут с
`allow_controlled_source_fallback=False`, поэтому завершение — по построению; лестница включается,
только если попросит МЕНЬШЕ, так что блок, прошедший с первой попытки, не стоит ни одного лишнего
вызова. Абзац, который она не вытянула, получает статус `omitted`, а не `source_restored`, —
озвучка первый удерживает, второй произносит. **Счётчики** — два семейства: пайплайновое
`controlled_block_fallback_*` (`16e5358`, PR #64) и `degradation_ladder_*` (`2c26d22`), оба доходят
до `..._summary.txt` прогона; разложение — в `docs/LOGGING_AND_ARTIFACT_RETENTION.md`.

Что НЕ влито и остаётся объёмом спеки: **ступень 4** (другая модель) — `_attempt_degradation_ladder`
своим docstring объявляет себя «ступенями 2 и 3»; **инвариант** — таблица
`CONTROLLED_BLOCK_FAILURE_POLICY` (`pipeline/block_execution.py:21-38`) по-прежнему отдаёт всем семи
качественным отказам `fallback_continue`, то есть доставка непереведённой прозы остаётся выразимым
исходом; **дыра в озвучке** — ветка подстановки на `:988-991` по-прежнему не делает ни одного
`narration_chunks.append`, лестница лишь стоит перед ней и до неё доходит реже; и **вся часть B**.

**Лестница подтверждена платным прогоном 2026-08-08** (Money & Sustainability, аудиокнига, маркеры
включены, `runs/20260808T_ladder_verification/`): блоков с подстановкой исходника **2 → 0**, символов,
выброшенных из озвучки, **5 581 → 0**, `narration_char_count` 456 612 → 467 791. Приёмка не изменилась
— те же две упавшие проверки. Цена прогона $0,5136 → $0,5247: лестница сработала на одном блоке,
стоила три вызова и не оставила ни одного неспасённого абзаца. **Сравнение при этом не чистое, и это
надо помнить, а не округлять до победы:** на прошлом прогоне контракт маркеров сломался на двух
блоках, на этом — на одном, потому что модель отвечает по-разному. Из двух прошлых отказов лестница
спасла один; второй просто не случился.

**«Полсотни недоставленных переводов» — гипотеза опровергнута по артефактам 2026-08-08; третьего пути
потери не существует.** Двадцать девять случаев на документном пути **есть в доставленном DOCX**: их
не находил разовый скрипт дампа, потому что при сборке меняются префиксы и пробелы, — дефект был в
измерителе, а не в пайплайне. Семнадцать на аудиокнижном пути — это **блоки, выброшенные целиком**, а
не пропавшие внутри блока абзацы: в money_sustainability из 12 абзацев этих блоков отсутствуют 11, в
rethinking_money из 23 — 18. Спеку под это не заводить: терять нечего, а классов потери прозы
по-прежнему два, и оба названы выше.

**Спека 056 — контракт маркеров абзацев неудовлетворим. В РАБОТЕ: главное влито 2026-08-05, одно
решение не построено. Не «не начато».** Этот раздел до 2026-08-07 утверждал «не начато», хотя работа была влита (PR #41,
`5393db2`) через день после того, как ровно такое же расхождение чинили на спеке 055. Гард не поймал
и не мог: он сверял статус спеки с этим файлом, но в `git` не ходил, а здесь оба документа врали
согласованно. **С 2026-08-07 ходит** — правило и три его уточнения описаны выше, в разделе «Проверить
статус любой спеки»; там же сказано, почему номер спеки в теме коммита теперь имеет цену. Цитата
прошлой формулировки в кавычках («не начато») утверждением не считается — иначе этот абзац не мог бы
рассказать, что здесь было написано неправильно.

Суть дефекта: промпт аудиокниги велит модели удалять абзацы из ссылочного мусора; user-промпт
(`generation/_generation.py:532`) запрещает отвечать заглушкой; валидатор (`:806`, `:867`) запрещает
отвечать пустотой. **Разрешённого ответа не существует ни одного** — блок отвергался при любом
поведении модели. Второй отказ той же формы: `:654` запрещает разрыв абзаца, а правила 5 и 11
системного промпта его требуют. Цена на первом прогоне: 6 блоков из 237, **20 725 символов
английского исходника в русской озвучке**, и **все 34** маркерных ретрая пересылали байт-в-байт
одинаковый запрос (черновик спеки говорил «~28»; спека сама себя поправила измерением, `:204`).

Что влито: **E** — типизированная диспозиция на каждый абзац (`1faeb06`), это и есть замена всем
четырём решениям черновика; и **D′** — отвергнутый ответ записывается внутри цикла попыток
(`816a0eb`). Плюс три P0-правки по ревью той же работы (`cbe0084`, `c4dfce8`, `730cf0f`). Что НЕ
влито: **C′** (сообщать модели нарушение на первом ретрае) — коммита нет, а
`_build_marker_recovery_user_prompt` последний раз менялся `6bc8ff9`, до PR #41. **A** отозвано как
необоснованное, **B** отложено за E — оба решения приняты вторым проходом спеки (`:153`, `:208`,
`:239`), и пересказывать их как действующие нельзя.

**Спека 054 — режим аудиокниги. В РАБОТЕ, прогон сделан 2026-08-04.** Шаг «ревью кода» дал четыре
находки, все закрыты (PR #32/#33/#34/#37): регион источников и примечаний теперь действительно
выбрасывается, роль `toc_entry` перестала выдумываться из формы строки, валидатор больше не убивает
оплаченный прогон, а в озвучку не попадает ничего, кроме произносимого текста на целевом языке.
**Прогонов было три, а не один.** Первый — Money & Sustainability, en→ru, $0.53, 14 минут,
`artifacts/audiobook_first_run/`. Он переставил приоритеты: самым дорогим дефектом оказался не импорт,
а подстановка исходного текста при провале валидации (4,47% книги по-английски, починено). Затем
`artifacts/audiobook_four_book_run/` и подтверждающий прогон 2026-08-06 —
`artifacts/audiobook_final_run/`, пять прогонов суммарно, $2.55 и 6,1 млн токенов
(`final_package.md:3-4`). **Осталось**: владелец читает материал глазами и выносит вердикт — мерило
прежнее, сколько ручной правки удалось убрать.

Два хвоста, которые этот раздел называл незакрытыми, **починены 2026-08-06**: якорь региона задней
части перестал требовать заголовка (`85fd8c5`), а граница ссылочного региона теперь кончается там, где
начинается следующая секция, а не там, где велит уровень заголовка (`19f8d1c`). Замерено: блоков,
исключённых как `reference_region`, стало 20 вместо 0; NOTES 264 из 264 вместо 0, BIBLIOGRAPHY 177 из
177 вместо 0 (`artifacts/audiobook_final_run/final_package.md:47-50`). Спека 055, в которую они
упирались, закрыта.

**Спека 050 — двухколоночные PDF. ОТЛОЖЕНО решением владельца 2026-07-31, без даты. Не поднимать
самостоятельно.** Ревью раунда 10 (находка F6) утверждало, что двухколоночные PDF читаются
вразнобой, но это вывод из чтения кода, а не из прогона: корпус состоит только из одноколоночных
книг. Проверить это нечем — **у владельца нет двухколоночного PDF**. Двухколоночный документ, с
которым он работал, — это DOCX, а он идёт совсем другим путём импорта и на вопрос спеки ответить не
может. Поэтому вопрос припаркован: ничего не реализуем, ничего не планируем, но и не забываем — если
однажды появится настоящий двухколоночный PDF, начинать с раздела «Decision plan» в спеке. Спека
намеренно оставлена со статусом `DEFERRED` (то есть открытой), чтобы гард продолжал требовать её
упоминания здесь. Почему нельзя «просто починить по коду»: спека 049 ровно так и кончилась — идею
реализовали по безупречной трассировке кода, замерили (0 восстановленных заголовков, 1 потерянный) и
откатили.

**Читательская вычитка: прогнана живьём 2026-08-02, набор операций сужен 2026-08-03.** Сам проход
остаётся выключенным (`DOCX_AI_READER_CLEANUP_ENABLED=false`) — включать его отдельное решение.
Что выяснилось при разборе и подтвердилось прогоном: **проход физически не может переписывать
формулировки** — все его операции либо удаляют блок, либо переставляют текст, который уже есть в
исходнике, со сверкой символов. Это структурный уборщик, а не редактор. По итогам замера дефолтный
набор сужен до `normalize_heading_boundary` + `join_fragmented_paragraph`; остальные операции
остались в коде и включаются явной настройкой. Цифры и разложение пользы и вреда — в разделе ниже.

**Осознанно оставленный долг раунда 12** — перечислен в
[`specs/051-round12-premerge-remediation/spec.md`](../specs/051-round12-premerge-remediation/spec.md),
раздел «Not fixed here — deliberate residue». Это четыре мелких дефекта (P3), каждый с указанием
места; ни один не блокирует. Два вопроса, которые тот же раздел адресовал владельцу, с тех пор
решены: локальный `.env` переведён на `DOCX_AI_READER_CLEANUP_ENABLED=false`, а потерю коротких
заголовков на PDF-книгах владелец принял как потолок PDF-входа — см. следующий раздел.

**Часть тестов не выполняется в CI никогда.** `.github/workflows/ci.yml` снимает маркеры
`system_deps` и `manual_ai_heavy` и ставит `DOCXAI_SKIP_WORKFLOW_SMOKE=1`. Причина законная — на
раннере нет LibreOffice, а часть тестов ходит в платные модели, — но покрытием это не становится.
Сколько тестов выпадает по маркерам, покажет

```bash
bash scripts/test.sh tests/ -q --collect-only -m "system_deps or manual_ai_heavy"
```

плюс те, что пропускает переменная `DOCXAI_SKIP_WORKFLOW_SMOKE`. Оба маркера настоящие: тесты по ним
есть, и команда выше это показывает.

**Браузерного покрытия UI нет вовсе, и маркер `browser_ui` убран 2026-08-08.** Он был объявлен в
`pyproject.toml`, снимался в `.github/workflows/ci.yml` и повторялся в контрактном тексте
`tests/test_script_contract_static.py`, `scripts/docker-ci-parity.sh` и `docs/testing/README.md` —
но его не нёс ни один тест: `--collect-only -m browser_ui` по всему набору выбирал ноль элементов.
То есть строка `not browser_ui` защищала пустое множество и при этом читалась как «покрытие есть,
просто отключено». Обозначавшего несуществующее маркера в репозитории больше нет. Если браузерное
покрытие заведут, маркер вернётся **вместе с тестами, а не раньше**.

Отдельная задача со статическими
проверками (`-m static_workflow`) здесь ни при чём: с 2026-07-31 она выбирает тесты по маркеру, а не
по списку файлов, поэтому новый файл с этим маркером выпасть из CI больше не может.

## Читательская вычитка: прогнана живьём, результат измерен (2026-08-02)

Первый в истории проекта живой прогон состоялся — три книги, настоящая модель, `$4.50` и 18,6 минуты
на все три. Считали не операции, а дефекты, которые видит читатель.

**Итог: 988 видимых дефектов до, 967 после. Минус 21, то есть 2,1%.** Из 51 тронутого места 25 стали
лучше, 15 — хуже. (База пересчитана 2026-08-07 по `artifacts/reader_cleanup_live_run/first_live_run_summary.json`:
255+304+429 = 988 и 249+292+426 = 967. Раньше здесь стояло 977/956 — числа, которых нет больше нигде
в репозитории; дельта и процент от этого не изменились.)

Разложение по операциям однозначно: `normalize_heading_boundary` и `join_fragmented_paragraph` дали
18 исправлений при 4 повреждениях, а `remove_inline_noise` и `delete_block` — **ноль измеримой
пользы и 12 из 16 повреждений**. Вред не косметический: из ссылки WHO вырезано имя файла, из
названия статьи вырезаны фамилии авторов, из библиографии — годы, из адреса издательства — номер
дома, со схемы — номера шагов. Читатель этого не заметит; заметит тот, кто пойдёт по ссылке.

**Класс проблем НЕ обобщается.** У каждой книги свой доминирующий дефект, и проход попадает по нему
только на одной из трёх; на mazzucato прогон вышел в минус. Зато общее у всех трёх — то, чего проход
не делает совсем: **572 приклеенные сноски и 305 битых пунктов списка**. Это самые крупные популяции
видимых дефектов, они не сдвинулись ни на единицу, и форма у них регулярная — это работа для
детерминированного правила на импорте, а не для прохода за 4 млн токенов.

Инструменты прогона в репозитории и воспроизводимы: `scripts/run-reader-cleanup-faithful-replay.py
--live` и `scripts/measure-reader-cleanup-visible-defects.py`, сводка — в `artifacts/`.

## Что делать дальше — оценка на 2026-08-02

**Решение владельца по вычитке: оставить только две операции, чинящие заголовки. СДЕЛАНО
2026-08-03.** Прогон показал, где польза, а где вред, и они разделены по операциям чисто. Набор сужен
до `normalize_heading_boundary` + `join_fragmented_paragraph` — это сохраняет все 18 исправлений и
убирает 12 повреждений из 16, включая весь вред по библиографии. Ключ
`reader_cleanup_allowed_operations` проведён через продовую конфигурацию (`resources/config.toml` →
`core/config_runtime_sections.py` → `core/config_loader_layers.py` → `AppConfig`) и по умолчанию
содержит эту пару; остальные операции никуда не делись и включаются явным списком в конфиге или
run-профиле (пустой список по-прежнему значит «все»). Офлайновый реплей на трёх книгах: принято
5/33/3 вместо 9/39/6, разница — ровно отклонённые `remove_inline_noise` и `split_block`
(`operation_not_allowed_by_cleanup_contract`), множества принятых заголовочных операций не изменились.

Оставшиеся 4 повреждения — дефект точки разреза внутри `normalize_heading_boundary`: он режет по
тире в биографической справке («ДЕННИС МЕДОУЗ — соавтор книг…») и оставляет висящую кавычку. Виден
глазом и обратим, чинить отдельно.

**Сноски и списки закрыты 2026-08-03.** Приклеенные маркеры сносок починены на импорте: правило
читало только хвост строки и имело порог размера 0,62, тогда как книги набирают сноски на 0,65–0,80 —
поэтому механизм срабатывал ровно на одной книге из четырёх. Стало 634 → 215 на трёх книгах.
Битые пункты списка чинить не пришлось — они оказались устаревшим замером, см. поправку ниже.

Спека 017 (штампы на сканах) закрыта 2026-08-02: детекция по каденции повторов, 167 штампов убрано
из скана, ноль срабатываний на остальных четырёх книгах.

### Поправка к цифрам прогона (2026-08-03)

Из 877 видимых дефектов, которые вычитка не трогает, **305 битых пунктов списка оказались
устаревшими**. Живой прогон крутил переведённый markdown, записанный 18 июня, а импортёр починили 22
июня — на четыре дня позже. На текущем коде их 8 на четыре книги, и из них 4 вообще не дефект
(слоган издательства). Поэтому вычитка «не сдвинула ни одного» — двигать было нечего.

572 приклеенные сноски были настоящими: их мерили на текущем импорте, а не по записи, и они
закрыты — 634 → 215 на трёх книгах.

Урок на будущее: **замер по замороженному артефакту показывает состояние на дату артефакта, а не
на сегодня.** У класса битых списков не было ни одного замера на уровне эффекта — только юнит-тесты
механизма склейки, — поэтому шесть недель никто не знал, что он уже закрыт. Теперь есть метрика
`torn_list_item_count` в каноническом гейте с порогом 0 на всех четырёх книгах.

**Требует вашего решения: чем защищать справочный материал.** Текущий подход — распознавать
оглавление, указатель и библиографию по ФОРМЕ отдельного блока (где стоят числа). За 2026-08-01 это
правило переписывалось трижды, и независимое ревью показало, что оно всё ещё не работает в обе
стороны: обычная проза кое-где получает иммунитет («The measured values were 1, 2, 3, 4.»), а
библиография не защищена вовсе — в записанном реплее видно, как из ссылки WHO вырезано имя файла
(`…/whr00_en.pdf?ua=1` → `…/ .pdf?ua=1`).

Это пятый признак того, что инструмент выбран неверно. Конституция VII прямо говорит: детекция по
РЕГИОНУ документа или структурной РОЛИ, а не по форме текста, — и в репозитории такая детекция уже
есть в другом месте (`_resolve_bounded_toc_region`, `_resolve_references_region_start` в
`validation/formatting_coverage.py`). Есть и более чистый продуктовый вариант: исключать справочные
регионы из ВХОДА прохода целиком — тогда их нечем испортить и они не тратят токены (сейчас блоки с
иммунитетом всё равно отправляются модели и защищаются только на валидации).

Пока решение не принято, чинить форму дальше не надо: четвёртая правка подряд в ту же эвристику —
это ровно та бесполезная работа, от которой предостерегает раздел выше. 2026-08-01 попытка сделать
форму точнее уже завела в репозиторий словарь слов и нарушила Конституцию VII; правку пришлось
упрощать обратно.

## Независимое ревью крупных пачек

После крупной пачки работ (не после каждой задачи) стоит отдать диапазон коммитов на независимое
ревью в локально установленный Codex — он ходит другой моделью и видит то, что не видит внутренний
цикл. Проверено 2026-08-01: Codex нашёл шесть настоящих замечаний в работе, уже прошедшей три
внутренних адверсариальных круга, включая функциональный дефект — эвристика оглавления защищала
короткую прозу, и анти-регрессионный тест на это проходил вакуумно.

Каждую находку проверять самостоятельно перед исправлением: из тех шести одна оказалась слабее
заявленного, а первая попытка исправить главную завела в репозиторий словарь слов.

## Принятые потолки — не переоткрывать

Здесь лежат вопросы, по которым решение уже принято владельцем. Каждый закрыт не «руки не дошли», а
осознанно, с цифрами. Если возникает соблазн предложить по ним улучшение — сначала прочитайте спеку.

**Короткие заголовки на книгах из PDF теряются, и это потолок PDF-входа** (решение 2026-07-31, спеки
[046](../specs/046-universal-short-heading-evidence/spec.md),
[049](../specs/049-pdf-import-preserves-font-size/spec.md),
[053](../specs/053-short-heading-evidence-measurement/spec.md)). Замерено дважды. Размер шрифта не
различает ничего: ни один из 30 потерянных заголовков не имел шрифта крупнее соседей — поэтому
спека 049, которая пыталась протащить размер шрифта, дала ноль. КАПС — анти-признак, он втрое чаще
встречается у мусора. Различает только асимметрия вертикального отступа и контраст
жирность/курсив/центрирование, но продовый импорт PDF записывает в промежуточный DOCX **лишь жирность
и курсив**, так что на реальном PDF от правила остаётся треть. Когда структура важна — подавать книгу
в DOCX, там типографика сохраняется полностью. Новых эвристик по заголовкам не предлагать.

**Двухколоночные PDF** — см. раздел «Что открыто»: вопрос припаркован, потому что проверить его не на
чем.

## Чем это НЕ является

**UI — не следующий шаг.** Он сделан спекой 013 (коммит `58aaf2a`, 2026-07-13) и доработан спеками
014–016. `docs/specs/GLOBAL_PLAN_2026-06-16.md` и `docs/HANDOFF_2026-07-11.md` утверждают обратное —
оба помечены как исторические, планировать по ним нельзя. GLOBAL_PLAN ценен как лог того, как
принимались решения, и только.

## Как обновлять этот файл

Тем же PR, который меняет положение дел. Если PR закрывает спеку, меняет направление или добавляет
известный долг — правка сюда входит в его объём. Файл должен оставаться коротким: он навигация, а
не летопись. Историю ведут `git log` и `CHANGELOG.md`.

И не возвращайте сюда числа, которые протухнут: вместо «столько-то тестов зелёные» пишите команду,
которой это проверяется, вместо «`main` сейчас такой-то» — ничего, потому что это и так покажет
`git log`.
